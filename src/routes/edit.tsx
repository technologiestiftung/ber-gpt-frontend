import React from "react";
import { Edit as EditUI } from "../components/edit/edit";
import { Layout } from "../layout/layout";

export const Edit: React.FC = () => {
	return (
		<Layout>
			<EditUI />
		</Layout>
	);
};
